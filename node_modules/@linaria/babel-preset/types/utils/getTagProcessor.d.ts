import type { NodePath } from '@babel/traverse';
import type { TaggedTemplateExpression } from '@babel/types';
import BaseProcessor from '@linaria/core/processors/BaseProcessor';
import type { StrictOptions } from '../types';
interface IState {
    file: {
        opts: {
            root: string;
            filename: string;
        };
    };
}
export default function getTagProcessor(path: NodePath<TaggedTemplateExpression>, state: IState, options: Pick<StrictOptions, 'classNameSlug' | 'displayName'>): BaseProcessor | null;
export {};
