export interface IPayRequest {
  domain: string,
  amount: string,
}
export interface IRegistrDomain {
  domain: string,
  period: number,
}
export interface ITransferDomain {
  domain: string,
  pkey: string,
}
