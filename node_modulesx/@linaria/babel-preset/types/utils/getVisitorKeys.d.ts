import type { Node, VisitorKeys } from '@babel/types';
declare type Keys<T extends Node> = (VisitorKeys[T['type']] & keyof T)[];
export default function getVisitorKeys<TNode extends Node>(node: TNode): Keys<TNode>;
export {};
